#!/bin/sh
set -e
# Automatically added by dh_gconf
if [ "$1" = remove ] || [ "$1" = upgrade ]; then
	gconf-schemas --unregister gnome-volume-control.schemas gnome-sound-recorder.schemas gnome-audio-profiles.schemas 
fi
# End automatically added section
