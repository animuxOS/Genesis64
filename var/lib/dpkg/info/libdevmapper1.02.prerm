#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/libdevmapper1.02" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d libdevmapper1.02 stop || exit $?
	else
		/etc/init.d/libdevmapper1.02 stop || exit $?
	fi
fi
# End automatically added section
