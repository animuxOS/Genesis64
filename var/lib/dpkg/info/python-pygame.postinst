#!/bin/sh
set -e
# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-pygame
	if grep -qs '^python-pygame$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-pygame$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
